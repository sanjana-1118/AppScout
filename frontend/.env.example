# AppScout Frontend Environment Configuration
# Default: Uses relative '/api' route (ideal for dev server proxy and same-domain reverse proxies)
# For remote or separate frontend deployments (Vercel, Netlify, Cloudflare Pages, S3):
# VITE_API_URL=https://your-api-domain.com/api
